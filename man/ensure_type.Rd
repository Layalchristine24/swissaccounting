% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensure_type.R
\name{ensure_type}
\alias{ensure_type}
\title{Ensure type stability for data frame columns}
\usage{
ensure_type(.data, ..., .default = NULL, .call = rlang::caller_env())
}
\arguments{
\item{.data}{A data frame whose columns should be cast to specified types.}

\item{...}{Named arguments specifying the desired type for each column.
The names should match column names in \code{.data}, and the values should be
prototype objects that define the target type (e.g., \code{character()}, \code{integer()},
\code{numeric()}, \code{logical()}, or more complex types like \code{factor(levels = c("a", "b"))}).}

\item{.default}{Optional default type to apply to columns not explicitly specified
in \code{...}. Can be a prototype object or an expression that evaluates to one.}

\item{.call}{The call environment for error reporting (used internally).}
}
\value{
A tibble with columns cast to the specified types. Only the columns
mentioned in \code{...} (and those covered by \code{.default} if provided) are returned.
}
\description{
Casts columns in a data frame to specified types, with error handling and
informative error messages. This function ensures type stability by converting
columns to their expected types.
}
\examples{
\dontrun{
df <- data.frame(
  x = c("1", "2", "3"),
  y = c("TRUE", "FALSE", "TRUE"),
  z = 1:3
)

# Specify types for specific columns
ensure_type(df, x = integer(), y = logical())

# Use a default type for unspecified columns
ensure_type(df, x = integer(), .default = character())
}
}
