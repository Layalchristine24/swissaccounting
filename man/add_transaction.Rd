% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_transaction.R
\name{add_transaction}
\alias{add_transaction}
\title{Add a Complete Transaction (Debit + Credit Pair)}
\usage{
add_transaction(
  ledger_file,
  date,
  descr,
  debit_account,
  credit_account,
  amount
)
}
\arguments{
\item{ledger_file}{Path to the ledger CSV file}

\item{date}{Transaction date (character or Date object)}

\item{descr}{Transaction description}

\item{debit_account}{Account number to debit}

\item{credit_account}{Account number to credit}

\item{amount}{Transaction amount (positive number)}
}
\value{
Invisibly returns NULL. Side effect: updates the ledger CSV file
}
\description{
Simplified function to add a complete double-entry transaction with one call.
This function adds two ledger entries (debit and credit) automatically, maintaining
the exact same behavior as manual calls to \code{add_ledger_entry()}.

The function creates two entries:
\itemize{
\item{First entry (debit): Creates an entry with self-linked counterpart_id}
\item{Second entry (credit): Automatically links to the first entry via counterpart_id}
}

This ensures proper double-entry bookkeeping and maintains Swiss GAAP compliance.
}
\examples{
\dontrun{
# Define ledger file path
ledger_file <- "path/to/ledger.csv"

# Add a simple transaction
add_transaction(
  ledger_file = ledger_file,
  date = "2024-01-15",
  descr = "Office supplies",
  debit_account = 4000,
  credit_account = 1020,
  amount = 500
)

# Add service revenue
add_transaction(
  ledger_file = ledger_file,
  date = "2024-02-15",
  descr = "Consulting services",
  debit_account = 1020,
  credit_account = 3000,
  amount = 3000
)
}
}
